# Add a Hello Page

This spec describes adding a simple "hello" page to the Next.js MDX blog site.

## Tasks

- [ ] Create a `/hello` page that displays a friendly greeting message

## Implementation Details

The page should:
- Be a simple Next.js page at `app/hello/page.tsx`
- Display a "Hello" heading and a friendly welcome message
- Match the existing site styling
